/* global solution, describe, it, expect, should */

describe('multiplesThreeFive', function () {
  'use strict';

  it('exists', function () {
    expect(multiplesThreeFive).to.be.a('function');

  });

  it('does something', function () {
    expect(true).to.equal(false);
  });

  it('does something else', function () {
    expect(true).to.equal(false);
  });

  // Add more assertions here
});

describe('largest prime factor', function () {
  'use strict';

  it('exists', function () {
    expect(multiplesThreeFive).to.be.a('function');

  });

  it('does something', function () {
    expect(true).to.equal(false);
  });

  it('does something else', function () {
    expect(true).to.equal(false);
  });

  // Add more assertions here
});

